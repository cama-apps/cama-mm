"""Domain data modules for the /dig tunnel-digging minigame.

The original single ``services.dig_constants`` module was split into cohesive
submodules here. ``services.dig_constants`` remains as a thin re-export facade
so existing ``from services.dig_constants import ...`` imports keep working.
"""
