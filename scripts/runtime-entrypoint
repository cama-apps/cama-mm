#!/bin/sh
set -eu

expected_runtime=$1
shift

if [ "$expected_runtime" != rust ] || [ "${BOT_RUNTIME-rust}" != rust ]; then
  echo "runtime image only supports BOT_RUNTIME=rust" >&2
  exit 64
fi

exec "$@"
